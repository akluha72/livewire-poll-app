1. First livewire component
    -> create component using artisan command
    -> php artisan make:livewire CreatePoll
    -> two folder will be created livewire controller and the livewire blade
    -> use the livewire directive to create a component
